"""support_agent: a trustworthy AI customer-support agent for a single Twitter brand.

The package is intentionally dependency-free (Python 3.10+ standard library only) so
the headline results reproduce anywhere in well under 15 minutes. All heavier options
(hosted LLMs, transformer embeddings) are optional and plug in behind clean interfaces.
"""

__version__ = "0.1.0"
